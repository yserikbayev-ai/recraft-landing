# CLAUDE.md — REFACTOR (лендинг, refactor.kz)

## Что это за папка

Кодовый проект (статический сайт). Одностраничный лендинг консалтингового бренда **REFACTOR** — финансовый контроль и цифровой порядок для промышленных компаний Казахстана (управленческая отчётность, закупки, учётные системы, AI поверх 1С). Прежнее имя бренда RECRAFT выведено из обращения — в текстах не использовать; бэкап прежней версии лежит в `index.html.bak-recraft`. Это git-репозиторий; действующий домен обслуживается Caddy на финском сервере, GitHub Pages сохраняется как копия.

От Claude здесь обычно требуется: правки вёрстки/контента/стилей, доработка анимаций и адаптива, помощь с деплоем на GitHub Pages.

> README.md в папке уже есть — он первичный источник по проекту. Не переписывать его.

## Технологии / стек

- Чистый HTML / CSS / JS, без фреймворков и сборщиков.
- `index.html`, `styles.css`, `app.js`; данные `projects.json`, статические страницы `projects/`, локальные ресурсы `assets/`.
- Дизайн: светлый фон, графит и оранжевый акцент, локальный Onest, оригинальная объёмная иллюстрация, мобильная адаптивность и reduced-motion.
- Деплой домена: Caddy на `claude-server`, каталог `/opt/recraft-landing` → `/srv`. GitHub Pages (`main` / root) — копия; push не обновляет действующий домен.

## Ключевые факты

- Главная и 35 страниц кейсов. `og.png` — сохранённое превью. Генератор: `python3 scripts/build-site.py`.
- **Опубликован:** https://refactor.kz (Caddy на финском сервере; repo yserikbayev-ai/recraft-landing; GitHub Pages main/root сохраняется как копия).
- Позиционирование: «Сложный бизнес. Ясная система» — финансовый контроль, операционные процессы и AI.
- Структура: hero → направления → 6 визуальных кейсов → каталог 35 проектов → AI-практика → подход → основатель → FAQ → бриф и контакты.
- Контакты: Telegram @yerzhan_serikbayev, email yserikbayev@gmail.com, LinkedIn /in/yerzhan-serikbayev, Instagram @yerzhanserikbayev (в футере).
- SEO/шеринг: canonical, og:image + twitter:card на `og.png`, JSON-LD ProfessionalService.
- НЕЛЬЗЯ: имена клиентов, суммы контрактов, маржа (NDA); упоминание Alstom как работодателя. Цены на сайте не публикуем — только структура оплаты («фиксированная цена этапа», «постоплата по акту»).
- Это git-репозиторий (есть `.git/`). Коммитить/пушить — только по явной просьбе пользователя.
- Секретов в проекте нет.

## Как со мной работать здесь

- Всё на русском.
- README.md уже существует — не перезаписывать; при изменениях проекта обновлять согласованно.
- Сохранять подход «без фреймворков»: правки делать в `index.html`, не тащить зависимости без необходимости.
- Git-операции (commit/push) — только по запросу; при работе в git сначала ветка, если на основной.

## Основная версия — 16.09.2026

По прямому указанию пользователя версия портфолио перенесена из `/v2/` в корень.
Каталог V2 удалён, старые адреса перенаправляются Caddy без префикса `/v2`.
Публичные данные — `projects.json`, сборка — `python3 scripts/build-site.py`.
На сервере публиковать только файлы сайта; не удалять соседние приложения и конфигурацию.
Актуальная схема публикации — README, решения — ASSUMPTIONS.md.
